 I'm putting all my dependancies in a container so that anybody who uses my pipeline doesn't
have to install muscle and any the other dependencies. As long as the user has sudo privelages they shouldn't have any trouble running this locally. They will just need to have podman/docker/apptainer.
